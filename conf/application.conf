# https://www.playframework.com/documentation/latest/Configuration

encry {
  postgres {
    url = ""
    username = ""
    password = ""
  }
}

play.filters {
  enabled += loggingSystem.LoggingFilter
}