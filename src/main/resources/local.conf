{
  encry {
    # App data directory
    directory = ${user.dir}"/encry/data"

    restApi {
      bindAddress = "0.0.0.0:9052"
      corsAllowed = true
      timeout = 5s
    }

    postgres {
      host = "jdbc:postgresql://172.16.10.55:5432/encry_explorer"
      user = "db_admin"
      password = "Yb2e-oii8-T3bo-pT8Z"
    }
  }

}