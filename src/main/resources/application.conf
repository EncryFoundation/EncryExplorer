{
  encry {
    # App data directory
    directory = ${user.dir}"/encry/data"

    restApi {
      bindAddress = "0.0.0.0:9052"
      corsAllowed = true
      timeout = 5s
    }

    postgres {
      host = ""
      user = ""
      password = ""
    }
  }

  akka {
    log-dead-letters = 0
    log-dead-letters-during-shutdown = off
  }
}